import HeaderAnimated from "@/components/Header-Animated";
import React from "react";

const Loading = () => {
  return (
    <div>
      <HeaderAnimated className={"text-blue-400/30"} title={"LOADING..."} />
    </div>
  );
};

export default Loading;
